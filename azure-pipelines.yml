trigger:
  - main  

pool:
  name: local-pipeline-agent
steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '18.x'
    displayName: 'Install Node.js'

  - script: |
      npm install
    displayName: 'Install dependencies'

  - script: |
      npm start
    displayName: 'Run app'